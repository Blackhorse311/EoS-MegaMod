# EoS_dev

> Empyrion mod (MegaMod) with Python tooling. Populated from baseline v1.4.0 (2026-07-05) — grow this file as the project evolves.

## Rules and Standards

- `.claude/rules/` **auto-loads** at session start: `core.md` always; language rule files
  activate only when working on matching files. Project-specific rules and Do-NOT-Change
  entries belong in `rules/core.md`.
- `.claude/standards/` is **on-demand reference** (review taxonomy, error handling,
  testing, git conventions). Not auto-loaded.


## Skills

`/quick-review` (fast SEC/REL scan) · `/full-review` (all categories, forked context) ·
`/fix-issues` (apply review fixes, user-invoked) · `/legends-review` (five-legend Workflow
review on Fable) · `/review-help` (cheat sheet). Built-ins: `/code-review low..ultra`,
`/security-review`, `/simplify`, `/verify`.

## Architecture / Key Decisions

*(record decisions here or in `docs/adr/` as they happen)*
